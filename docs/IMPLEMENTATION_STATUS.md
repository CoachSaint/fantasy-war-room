# Implementation status

Updated: 2026-09-25

## Decision

**Status: HOLD for full production distribution.** The repository is suitable for a protected demo and an integration preview. It is not evidence of a fully live, current, multi-tenant fantasy intelligence service until the configuration and live gates below are completed.

### 2026-09-25 current candidate and review

- The original deployment design is Vercel plus one **hosted Supabase Free** project (`fswsefqqlltmaktiqwge`). The Supabase CLI links to that project to apply migrations; production does not require local Docker. The hosted database measured 19 MB on 2026-09-25 against the currently published 500 MB Free database allowance. Organization Free plan and no paid add-ons were independently checked on 2026-09-24, not rechecked today. Season-long ledger growth remains unmeasured.
- Local and hosted migrations `0001`–`0017` are aligned. Migration `0010` serializes imports of a shared Yahoo league across managers and preserves a confirmed roster owner against stale writes. A hosted three-user fixture passed two-manager league mapping, concurrent lease refusal, owner preservation, owner-theft refusal, and outsider lease refusal. The fixture used synthetic Yahoo data, not owner OAuth.
- Migrations `0011`–`0012` write Yahoo decision and weekly-point prediction ledgers transactionally when recommendations are published, retain old recommendations on refresh, protect recorded facts against updates, and deny direct authenticated ledger writes. Migrations `0013`–`0015` preserve the forecast source and Yahoo scoreboard observation, reject a late/previously observed game forecast from measured accuracy, store source-linked outcomes only after Yahoo advances the week, and deduplicate identical refresh forecasts in MAE/RMSE. Astra identified that scoreboard status alone could certify a forecast after an individual player's kickoff. Migration `0016` now requires a fresh nflverse schedule for that player's NFL team and a forecast timestamp before kickoff, freezes the league scoring rules, retires earlier certifications, and adds a cursor for bounded outcome pages. Scout acquires the shared Yahoo league lease for lineup, waiver, outcome, and brief materializers. The points outcome uses nflverse raw game stat columns under the **frozen** Yahoo coefficients, never its generic PPR total as a custom-league result. A hosted disposable fixture passed expected three unique forecast errors (`-2`, `+3`, `-2`, MAE `2.333`, RMSE `2.380`), an after-kickoff forecast refusal, a changed-league-scoring control, idempotent reconciliation, unverified late forecast refusal, owner/outsider/anonymous boundaries, and cleanup. This is synthetic acceptance; no real league outcome or calibration result exists. Snap/route/availability outcomes and calibration remain incomplete.
- Clean reviewed code commit `375cd2620a31cf303672be7a67b4404d48e8e771` was staged as protected READY deployment `dpl_D7JgQYWWtHZTigoyZc69Ffdvwrpo`. Astra reviewed the first outcome code commit `fe23a34270eaaf2dc124ec80d3954197c821af6f` and found four P1 risks. Corrected clean commit `db1549920ea717fa511d8694aebed8dc75f79d01` passed 93 local tests (four hosted fixtures skipped in the ordinary run), lint, TypeScript, local/remote production builds, and the separate disposable hosted lineup/outcome fixture. Its protected READY deployment is `dpl_Gp385gXRQvGRp6Sc6NMqoS3YvqNL` (`https://fantasy-war-room-mivqagkyw-coachsaints-projects.vercel.app`); protected health returned HTTP 200 with hosted Supabase connected and demo mode true, and anonymous Accuracy returned HTTP 401. Astra's read-only re-review found no remaining P0/P1 in the four corrected scenarios and passed this protected preview. It did not rerun hosted fixtures and held full production acceptance. CLI deployment SHA binding is operator-verified from the clean checkout and command; Vercel supplied no independent Git source metadata. Read-only inspect confirmed the public `fantasy-war-room-pi.vercel.app` alias remains on fallback `dpl_E8FFiYzfonqeR4npvGGQqg2jYYtg`. PR #2 now tracks this branch as a draft and remains unmerged. No public alias promotion or Yahoo refresh activation occurred.
- Mason's exact login email and owner Yahoo authorization have not been supplied. No real Yahoo roster, current-week matchup, or multi-manager import has been accepted, and scheduled Yahoo refresh is not active. Do not label the application fully live or complete until those checks pass.
- Manual league setup now calls migration `0017` as a single Postgres transaction instead of writing and compensating across multiple HTTP requests. A hosted disposable fixture passed authenticated creation of the complete workspace/league/roster/scoring/assignment graph, owner creation of a second league in that workspace, outsider refusal, direct authenticated RPC refusal, and total rollback after an injected invalid assignment late in the transaction. It cleaned up all fixture workspaces, leagues, memberships and assignments. This confirms the manual setup path, not a real Yahoo import. Code commit `ae477633414be052ca11ece23f89c4eb892573d3` passed 93 ordinary local tests, lint, TypeScript, local and remote production builds. Protected READY deployment `dpl_A2fvwdaUikiv9oqR5yzAxnXxU2hS` returned health HTTP 200 with hosted Supabase connected and demo mode true; a valid anonymous setup request returned HTTP 401. A later PR-CI check failed while the ordinary Vitest run reported five skipped credential-gated fixtures; `vitest.config.mjs` now excludes those five from the ordinary gate and keeps them available for explicit `FWR_LIVE_TEST=1` hosted runs. The Node 20 local full CI script passed 93/93 tests, zero failed and zero skipped, lint and build after that correction; the remote PR-CI rerun subsequently passed at `ad06545`. The public alias remains on the fallback. Astra's final review of the newer candidate remains pending before shipping.
- PR-CI on code/docs head `ad06545ef32186b7d276d586b63823a40b8c96b3` subsequently passed the native Cloud Build check and Vercel check. Dependency preflight identified advisories on the pinned Next.js, Sharp, Vitest, and js-yaml versions. Clean code commit `fcf7e84dffb2cc9eb313969cd36b6173489d8b66` pins Next.js and ESLint config at `16.3.6`, Vitest at `4.1.11`, updates the transitive Sharp and js-yaml fixes, and moves active Cloud Build from Node 20 to Node 22 to match the current Supabase SDK engine requirement. Its exact-SHA Node 22 local full gate passed 93/93 ordinary tests, zero failures/skips, lint, TypeScript and build; `npm ci` audited 416 packages with zero reported vulnerabilities. Separate hosted manual-setup and Yahoo-lineup fixtures passed. Protected READY deployment `dpl_HDyDPwTziQ6G5L932UZNjGvohf75` at `https://fantasy-war-room-m5n4vf1ce-coachsaints-projects.vercel.app` passed its remote build; health returned HTTP 200 with hosted Supabase connected and demo mode true, and anonymous Accuracy and wrong-bearer Scout requests both returned HTTP 401. The PR's Vercel and native Cloud Build checks passed on this code commit. The public `fantasy-war-room-pi.vercel.app` alias remains on fallback `dpl_E8FFiYzfonqeR4npvGGQqg2jYYtg`. CLI SHA binding is operator-verified from the clean checkout and deploy command, not independent Vercel Git source metadata. Final Astra review and external owner acceptance remain pending before shipping.
- Five connected pages previously rendered demo content whenever league context was unavailable while demo mode was enabled. Clean code commit `3d33c254f493ab51dcf52d36c3a1f1b9a3a2a98b` now shows demo only to signed-out visitors; authenticated users without a league see setup guidance and context failures show an error. Its exact-SHA Node 22 gate passed 108/108 ordinary tests, zero failures/skips, lint and build; the added 15 render checks cover all five pages in unavailable, setup-required and signed-out states. The protected READY deployment `dpl_4XVpZAzErjS5yuSozHapnBEorEJt` at `https://fantasy-war-room-mtgf1y16d-coachsaints-projects.vercel.app` passed remote build, health HTTP 200 with hosted Supabase connected and anonymous Accuracy HTTP 401. A wrong Yahoo refresh bearer returned HTTP 401; the current bearer retrieved privately from Supabase Vault returned HTTP 200 `no_consented_connections` without exposing its value. The positive Scout bearer reached materialization and returned HTTP 503 `current_yahoo_league_unavailable` after its initial steps succeeded. This is an expected lack of live owner data, not a successful current-league Scout run. Native Cloud Build and Vercel PR checks passed on this code commit. Read-only hosted counts were zero auth users, Yahoo connections, Yahoo leagues and Yahoo refresh jobs. The public fallback still returns HTTP 404 for the Yahoo refresh endpoint, so scheduling it before public promotion would fail. The public `fantasy-war-room-pi.vercel.app` alias remains on READY fallback `dpl_E8FFiYzfonqeR4npvGGQqg2jYYtg`; no merge, promotion or schedule activation occurred. Final Astra review and real Yahoo owner acceptance remain open.
- History now lets the signed-in manager explicitly mark a saved recommendation accepted, ignored or overridden, add an optional note, or clear the response. This does not infer that a roster move occurred. The server route checks league membership and filters the immutable decision row by both league and authenticated user. Clean code commit `f4898a37eaf9b08d617209d7ba5d1383a4aea9aa` passed the exact-SHA Node 22 gate with 109/109 ordinary tests, zero failures/skips, lint and build. A disposable hosted fixture passed owner save/clear, anonymous and outsider refusal, second-manager isolation within the same league, immutable fact preservation, and a direct authenticated database update that returned zero rows and left the record unchanged. Cleanup returned auth users, leagues, decision events and provider connections to zero. Protected READY deployment `dpl_FaVMysvVX9ekLP3G2hChUbq1NAML` at `https://fantasy-war-room-8rn67i9t0-coachsaints-projects.vercel.app` returned health HTTP 200 with hosted Supabase connected and anonymous History PATCH HTTP 401. Native Cloud Build and Vercel PR checks passed on this code commit. The public alias still points to fallback `dpl_E8FFiYzfonqeR4npvGGQqg2jYYtg`. No real manager response or league import has occurred.
- An isolated 390 px Chrome run exercised the local candidate against the dedicated hosted Supabase project with a disposable Auth user. The browser passed signed-out demo, sign-in, signed-in setup gate, manual league creation, connected Today/Lineup/Waivers/Players/Draft/Roster/Accuracy/History, History response save and clear, sign-out, exact JTF attribution, no horizontal overflow, and no page exceptions. A browser screenshot exposed a context header mismatch: `/api/context` supplies the owned roster but no manager object. The header now displays the owned roster name instead of an unavailable manager placeholder and raw league UUID; the full browser run passed again after that correction. Read-only hosted counts after cleanup were zero Auth users, workspaces, leagues, memberships, recommendations, decision events, and provider connections. This was local-source browser QA with synthetic data, not a protected Vercel browser run or real Yahoo consent/import.

### 2026-09-24 activation check

- The dedicated Supabase project is on the **Free** plan with no selected paid add-ons. It has approximately 12 MB in a 500 MB database allowance. The application connects to the hosted project; a local Docker/Supabase CLI container is not required for production.
- Auth now points at the Vercel production URL, permits the exact `/auth/callback` redirect, and disables public signups. Administrator-generated invite links plus user-set passwords were proven with disposable users. The Supabase built-in mailer cannot deliver invites to arbitrary recipients without custom SMTP, so the operator must send the generated link directly to Mason's confirmed address after deployment.
- Connected roster and player APIs were proven against disposable owner/outsider/anonymous accounts. Owner reads succeeded, outsider and anonymous reads were denied, and all disposable rows/users were removed.
- The current nflverse weekly roster release provides exact Yahoo numeric player IDs and GSIS IDs. The candidate now joins imported Yahoo player keys to GSIS-backed canonical players when the mapping is unique. A disposable two-team Yahoo persistence fixture proved both roster assignments and a matchup, plus four provider IDs mapping to two canonical players; cleanup returned all affected table counts to zero. This is not a real Yahoo API consent/import proof.
- Yahoo settings normalization now reads only `stat_modifiers`, so a normal response containing both `stat_categories` and `stat_modifiers` does not reject category rows for lacking a numeric scoring value. A mixed-shape regression fixture passes. Connected League Settings displays the imported context; Coach excludes expired recommendations.
- The nflverse 2026 player-stats release currently contains regular-season weeks 1 and 2 while Sleeper reports week 3. Historical actuals are stored with their source week and never labeled as projections. Week 3 injury evidence is fetched separately. The large depth-chart asset is skipped under the bounded fetch limit.
- The candidate separately ingests current-week Sleeper projection fields for exact GSIS-matched players and keeps standard, half-PPR, and PPR values distinct. A disposable live Week 3 projection was persisted and repeated without duplicates, then removed. The public projection endpoint is observed but absent from Sleeper's published API reference; provider availability and non-commercial licensing remain release considerations. No projection accuracy claim has been established.
- Scout remains degraded: Yahoo consent/league sync, full snapshot diff, draft decisions, and full waiver/FAAB/three-week guidance are incomplete. The local candidate computes same-position Yahoo lineup swaps and narrow current-week add/drop pairs from source-matched Sleeper forecasts and imported Yahoo scoring modifiers. A disposable hosted-database control fixture proved one owner-scoped lineup swap, one available-player add/drop pair, replacement on rerun, a recommendation-change brief, and owner/outsider/anonymous read boundaries; it was cleaned up. The brief tracks materialized decision changes only. These checks do not establish forecast accuracy or a real Yahoo import. No real user league or recommendation has been imported or created.
- Migration `0005_yahoo_available_pool.sql` (SHA-256 `696b95953e8d94a009d3042fa98264045b70a32f83a0ecd10722a991da7cc019`) was applied to the dedicated hosted project after private public-schema/data dumps and a rolled-back SQL validation. The private pre-migration backups are `/Users/michaelsaint/.local/share/jtf/fantasy-war-room/rollback/2026-09-24-pre-0005-schema.sql` (SHA-256 `87c331cc87dd91339de7a893388d3b6665b26dab21ea9ce190f217308d59a905`) and `2026-09-24-pre-0005-data.sql` in the same directory (SHA-256 `32b076fb29ac377663db34f1048c47684b64a1e92a6f404eee867c98de32ba0b`). The migration list now shows local/remote `0001`–`0005` aligned. Yahoo sync can fetch at most 200 candidates per league from the official `status=A` collection, records whether the scan is truncated, and expires availability after six hours. No real Yahoo consent or live availability response has been observed. Post-fixture counts for users, leagues, players, evidence, recommendations, availability, and connections returned to zero.
- A manual Yahoo import now recomputes lineup and waiver advice from any already-ingested current-week forecasts and writes a new decision brief. The import response identifies leagues that were evaluated, still lack current inputs, or failed refresh; the setup UI keeps that result visible after its status reload. The hosted disposable control fixture passed this combined path. Scheduled Yahoo roster refresh and projection ingestion remain incomplete, so a manual import alone does not guarantee current advice.
- A bounded cron-authenticated Yahoo refresh endpoint now shares the manual sync lease and token-rotation path. Migration `0006_scheduled_yahoo_refresh.sql` enabled `pg_cron`/`pg_net` on the dedicated hosted Free project; local and remote migrations `0001`–`0006` align. A read-only query confirmed **zero** `fwr-yahoo-refresh` jobs. Scheduling requires deployment, the current `CRON_SECRET` in Supabase Vault, and a positive/negative endpoint check first; no scheduled Yahoo call or real consent has occurred.
- The connected recommendations API now filters expired/future-dated rows before its result limit, and a daily brief expires no later than the earliest action it contains. Today hides expired brief changes. The hosted disposable fixture proved an expired add/drop recommendation remains in storage for audit but is absent from the owner API response. Coach's launcher now follows the same primary-league selection as the connected pages.
- Coach now answers broad start/sit, add/drop, draft-availability, explanation, and decision-change questions directly from fresh persisted actions/briefs without requiring an optional model provider. It refuses unsupported position-specific answers from incomplete data and no longer falls back to demo advice when the connected context service fails. A hosted disposable fixture proved owner access and outsider/anonymous refusal for the structured Coach answer. Rich player-specific language questions still require the optional provider and live evidence review.

## Repository-complete work

- Five polished product surfaces plus explicit demo/configuration boundaries.
- League Setup Studio contract with strict input validation and canonical deterministic output.
- Exact scoring profile, roster slot, team/manager mapping, and player assignment DTOs.
- Fail-closed Sleeper roster identity and explicit active-pool availability calculation.
- nflverse official release-asset URL builder, CSV/JSON ingestion, season/week filtering, depth order, and practice participation preservation.
- Deterministic scoring, lineup optimization, seeded draft survival simulation, calibration metrics, and FAAB range helpers.
- Authenticated league setup persistence path and membership-aware recommendation/Coach reads.
- Constant-time cron bearer authorization, bounded request validation, Coach rate limiting, and explicit provider/database degradation responses.
- Initial schema plus workspace/membership/learning migration with RLS definitions.
- Credential-gated Yahoo OAuth, encrypted token persistence, normalized league/roster import, and setup UI are implemented. They have not been proven with a live authenticated import.

## Configuration or owner action still required

| Gate | Current boundary | Owner evidence required |
| --- | --- | --- |
| Supabase | Dedicated Free project `fswsefqqlltmaktiqwge` is healthy; the public alias is a connected demo fallback and newer candidate commits have been staged behind protection | Exact final candidate SHA/deployment binding, Mason login, and live access checks |
| Schema | `0001` through `0017` are aligned on the dedicated project; `0006` creates no refresh job; private full schema/data dumps predate `0005`, and later migrations have read-only metadata/count checks and disposable hosted fixtures | Recheck history before promotion and prove full user/membership isolation with real league data |
| League identity | Setup contract and persistence route exist | Authenticated real league setup, membership/roster mapping, persisted result |
| Setup atomicity | Migration `0017` provides a service-only transaction; hosted fixture proved full manual setup, outsider refusal and late-failure rollback | Repeat with real owner setup and inspect persisted league/roster mapping before public self-service use |
| Sleeper | Public adapter exists; no account/league is configured here | Real league selection, current NFL state, roster/player ID verification |
| Yahoo | Production Vercel reports Yahoo credentials configured, but no user has consented or imported a league; current review fixes remain local | Confirm Fantasy Sports: Read permission and exact callback, then prove Mason's consent and live import with his account |
| Yahoo matchups | Current-week scoreboard fetch, normalization, persistence, schema and member-only RLS are implemented; no real Yahoo response or import is yet proven | Complete an authenticated Yahoo consent/sync and verify roster and matchup rows/counts against Yahoo's current-week scoreboard |
| nflverse | Release assets and bounded parser exist; some seasons/assets may be unavailable | Current season/week asset availability, timestamps, sample data validation |
| Materialization | Candidate branch persists source-labeled global nflverse actuals, matched evidence, and current-week Sleeper forecasts; narrow Yahoo lineup, available-player add/drop, and recommendation-change brief paths passed a disposable hosted-database control fixture; full snapshot diff remains skipped | Repeatable live Scout run with real Yahoo consent, exact league scoring, waiver/FAAB and draft decisions, and source-backed actions across the release surfaces |
| Coach | Broad action questions use fresh structured league evidence without a model key; the optional OpenRouter path remains for richer explanations and is not proven with a real league | Optional provider/model policy, cost/rate checks, evidence-grounded live response, and human question trials |
| History response | Owner-scoped save/clear passed a disposable hosted fixture, including a second manager in the same league; direct authenticated ledger update changed no rows | Real manager uses History after a verified recommendation and confirms the recorded response and optional note |
| Vercel | Cron declaration, healthy public demo fallback, and protected READY stage `dpl_FaVMysvVX9ekLP3G2hChUbq1NAML` at code commit `f4898a3...` exist; the user-facing alias remains on fallback | Stage the final header correction, verify its exact SHA, then complete promotion, cron history and real owner checks |
| Yahoo refresh schedule | The cron-authenticated endpoint passed positive/negative bearer checks on the protected stage, and the secret exists in Vault; the hosted project has zero active Yahoo jobs. The public fallback lacks the endpoint | After promotion, activate the guarded job and inspect actual job/HTTP/provider results with a consented league |
| Rollback | Application rollback procedure is documented | Exact known-good deployment/SHA, database backup/PITR anchor, tested recovery |

## Truthful runtime states

- **Demo:** fixture data is shown intentionally and responses identify demo provenance.
- **Preview/configuration required:** the app directs the operator to connect Supabase/league state; no current recommendation claim is valid.
- **Degraded:** a required database/provider/materializer path is unavailable; HTTP and Scout step status must say so.
- **Connected candidate:** authenticated league and provider data exist, but release still needs live end-to-end evidence.
- **Production-ready:** only after exact source/live binding, migrations, RLS isolation, freshness, cron, rollback, and functional/negative checks pass together.

## Release checklist

1. Pin/install dependencies and run `npm test`, `npm run lint`, and `npm run build`.
2. Capture exact Git SHA and CI results; separate passed, failed, and skipped checks.
3. Configure only the documented environment names in the intended Vercel scope.
4. Apply migrations in order and capture declared versus pending results.
5. Verify Supabase Auth, workspace membership, league membership, and RLS isolation with separate users.
6. Complete a real league setup and verify canonical player/roster mappings.
7. Verify current Sleeper and nflverse source data; reject stale/empty data as current intelligence.
8. Run Scout with missing/wrong/correct cron credentials and inspect persisted steps.
9. Verify recommendations and Coach only return the authenticated league context; verify demo remains visibly demo.
10. Record deployment URL/ID, migration result, health output, cron history, provider freshness, rollback anchor, and final owner go/no-go.

The final go/no-go and owner-participation credential actions remain with the release owner. This document intentionally does not claim they have occurred.

Powered by JTF Software Solutions
